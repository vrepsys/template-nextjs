# Branding images
Instructions on how to change the branding images of the site.

## Favicon
I dont know where it can be changed.

## Logo
Follow the steps to change the logo of the site.

1. Go to **"src/components/navigation/topnav.tsx"**
2. Update `Logo` component
   
   ```jsx
   const Logo = ({ className }: { className: string }) => (
      <svg className={className} xmlns="http://www.w3.org/2000/svg">...</svg>
   );
   ```

## Social image

I dont know where it can be changed.

