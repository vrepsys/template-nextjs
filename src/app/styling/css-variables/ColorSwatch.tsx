export const ColorSwatch = ({ className } : {className?: string }) => {
  return (
    <div
      className={`border-portal-border inline-block h-6 w-6 rounded border ${className}`}
    ></div>
  )
}
